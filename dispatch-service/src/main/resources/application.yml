server:
  port: 8083

spring:
  application:
    name: dispatch-service

  datasource:
    url: jdbc:postgresql://localhost:5433/dispatch_db
    username: erp_user
    password: erp_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  rabbitmq:
    host: localhost
    port: 5672
    username: erp_user
    password: erp_pass

rabbitmq:
  exchange: erp.events
  dispatch-queue: dispatch.incident.queue

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
